# 크래시 증거 기록 양식

이 파일을 직접 덮어쓰기보다 사건별 문서에 아래 양식을 복사해 사용한다. 로그 원문 전체 대신 필요한 줄과 원본 위치를 기록한다.

## 사건 정보

- 사건 ID:
- 조사 일시:
- 조사자:
- 작업 모드: Review / Diagnostic / Implementation
- 사용자가 승인한 범위:
- 수정·실행 금지 범위:

## 기준 환경

- 저장소 절대 경로:
- Git 저장소 여부:
- 편집 전 branch / commit / dirty 상태:
- upstream 모드/버전/Workshop ID:
- HOI4 버전:
- 빌드/체크섬:
- 실행 파일 경로:
- 사용자 데이터 경로:
- 사용자 데이터 경로를 선택한 근거:
- 모드 이름과 버전:
- descriptor `supported_version`:
- 저장소 `descriptor.mod` 경로:
- launcher `.mod` 경로:
- launcher `.mod`의 실제 `path`:
- 런처가 실제 로드한 물리적 복사본과 확인 근거:
- `dependencies`:
- `replace_path` 전체 목록과 영향:
- `remote_file_id`:
- 저장소 / local-dev / Workshop 경로 구분:
- launcher playset 이름:
- 활성 모드 및 정확한 로드 순서:
- 필수 의존 모드 활성 여부:
- 활성 DLC:
- 게임 언어 / localisation 의존 설정:
- 실행 옵션:
- 기준 로그의 파일 수정 시각:
- 승인된 기준 로그 보존 위치와 실행 ID 또는 `NOT ARCHIVED`:

## 재현 절차

1.
2.
3.

- 기대 결과:
- 실제 결과:
- 재현 횟수 / 전체 시도:
- 마지막으로 정상 동작한 알려진 구성:

## 실행별 기록

| 항목 | 값 |
|---|---|
| 실행 ID | |
| 시작·종료 시각 | |
| 이전 실행과 다른 변수 | |
| 마지막 화면/진행 단계 | |
| 종료 형태 | |
| `error.log` 수정 시각 | |
| `game.log` 수정 시각 | |
| WER 보고서 경로 | |
| WER 이벤트 유형 | |
| 예외 코드 / 오프셋 | |
| 보고서 ID | |
| 결과 | PASS / FAIL / NOT RUN / BLOCKED |

## 핵심 증거

| 등급 | 출처와 줄/항목 | 관찰 내용 | 의미 |
|---|---|---|---|
| 확인됨 | | | |
| 강하게 뒷받침됨 | | | |
| 미확인 | | | |
| 반증됨 | | | |

- 사용한 읽기 전용 검색/비교 명령 또는 도구:
- 입력 파일 집합:
- 산출물 보존 위치 또는 `NOT ARCHIVED`:
- 로그 줄 번호와 짧은 발췌:
- 기준/control과의 차이:

## 가설 목록

| 가설 | 현재 등급 | 지지 증거 | 반대 증거 | 다음 분리 시험 |
|---|---|---|---|---|
| | | | | |

## 변경 제안

- 제안 범위:
- 보존할 동작/ID/밸런스:
- 대상 파일의 전체 경로:
- 예상 영향:
- 세이브·서브모드 호환성 영향:
- 안전 정지 또는 사용자 결정이 필요한 지점:
- 되돌리기 방법:

## 검증 결과

- 수행이 승인된 검사:
- 정적 검사 결과:
- 게임 스모크 테스트 결과:
- 이전 크래시 서명 재발 여부:
- 새 오류/회귀:
- 관련 bookmark별 결과:
- positive / 중요한 blocked·negative path:
- save/load 결과 또는 `NOT RUN`:
- AI runtime 결과 또는 `NOT APPLICABLE`:
- multiplayer checksum/sync 결과 또는 `NOT CLAIMED`:
- 남은 위험:
- 결론과 증거 등급:

## 변경 이력

- 변경한 파일:
- 변경하지 않은 관련 파일:
- Git 상태:
- 커밋/푸시/배포 여부:
