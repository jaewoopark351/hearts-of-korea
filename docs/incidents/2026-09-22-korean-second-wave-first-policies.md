# 2026-09-22 한국 2차 확장 첫 경제정책 묶음

> 후속 보상·사업 비용 조정은 [별도 변경 기록](2026-09-22-korean-policy-rewards.md)을 따른다. 아래 보상·비용·해시·149 PASS는 최초 구현 시점의 기록이며 후속 변경의 실행 검증 결과가 아니다.

## 범위와 기준선

사용자가 제시한 단계별 구현 계획에 따라 일반판 저장소에서 기존 326개를 조사·진단하고, 2차 전체 구성안과 첫 두 정책 묶음을 구현했다. 전체 450~470개 확장이 끝났다는 기록이 아니다. 기존 코드·이미지 정책 문서 3개의 미커밋 변경은 보존했다.

- 브랜치: `feat/korean-focus-expansion-ai`, 시작 커밋 `f43769cd9957d4f331b4d591506d52f5da485f79`.
- 대상: 설치본 HOI4 1.19.3.0.c01a, Steam build25205862, Windows.
- 실행 구성: 일반판 Hearts of Korea 하나, 활성 DLC36개, 새1936 KOR. 선언된 Korean Language는 미활성이었으므로 해당 의존성 구성 검증은 남는다.
- 기존 중점 소스 SHA256: `830c58121c359cd48d9343f312eb8b05c5cceee37e308e86f0cc74eed98ec579`.
- 적용 후 중점 소스 SHA256: `b32283d0a9220836a141e5f81aa63e05482652681224ea0c92ddf577aacf13bb`.

앞선 `-userdir` 격리 시도는 당시 활성화되어 있던 Road to 56 구성을 그대로 로드해 실패했다. 그 프로세스는 종료했으며 해당 실행은 일반판 검증으로 세지 않는다. 후속 작업에서 `dlc_load.json`이 일반판 저장소 하나로 바뀐 상태를 읽고 그 구성을 사용했다. 작업 중 활성 모드 목록·플레이세트·사용자 세이브를 변경하지 않았다.

## 구현

[첫10개 상세 명세](../HOK_KOREAN_SECOND_WAVE_FIRST_10_SPEC.md)와 [전체 구성안](../HOK_KOREAN_SECOND_WAVE_PLAN.md)을 기준으로 한다.

- 여운형 협동조합 5개, 박헌영 생산 배정 5개를 기존 부모의 옆가지로 추가했다. 총336개다.
- 두 정책 선택과 국민정신 I·II 8개를 연결했다. 상위 단계는 기존 혜택을 유지·강화하고 선택하지 않은 정책은 지급하지 않는다.
- 선택된 정책의 기간제 실행 사업4개를 추가했다. PP75·민공2·90일, 공통 동시1개, 종료·취소 후90일 대기다. 효과는 사업 중에만 적용하고 영구 공장을 반복 지급하지 않는다.
- 새18개 이미지, 중점 광택과 결정 재사용 매핑, 양쪽 언어 채널50키를 함께 연결했다. 정치색은 공산 경로의 고정 적색이다.
- 기존 신규60개 광택 오버레이를 프로젝트에 복사해 상대경로로 연결했다. 원본89개 아이콘 본체는 보존했다.
- 공산 구역의 공간을 위해 오른쪽 정치 기준점4개를 +10x 이동했다. 전체 x0~204·y0~24, 절대7개·상대329개다. 직접 offset 조건·이동량, 패널·바로가기는 보존했다.
- 실제 엔진이 거부한 개마고원·소양강댐의 뒤에 선언된 상대 기준을 앞선 기준점으로 바꾸었다. 계산된 기본 위치는 각각(2,5)·(8,5)로 유지했다. 같은 실수를 막는 선언 순서 규칙을 AGENTS.md에 기록했다.

기존326개의 ID·선행·상호배타·보상·비용·조건·AI는 위치 필드와 주석을 제외한 토큰 비교에서 동일하다. 기존 민주 역사 AI 계획은 수정하지 않았다. 새10개에는 기본 가중치2와 평시/전시 정책 선택 가중치만 추가했다. 미결 보병장비 생산량/생산비 대안은 적용하지 않았다.

## 정적 검증

| 항목 | 결과 |
|---|---|
| 중점 | 336개 고유 ID; 기존326개 게임 규칙 차이0 |
| 위치 참조 | 누락·순환·앞선 선언 위반0; 절대7·상대329 |
| 위치 모델 | 같은 중심 좌표0; 새10개와 주변 노드의 정적 직사각형 충돌0 |
| 정신·결정 | I→II 승계, OR 병합, 대칭 배타, 선택별 해금·시작/취소/종료 플래그 검토 |
| 이미지 |18 DDS의 크기·BGRA32·알파·해시 및 내부 상대경로 통과; 실제 크기 접촉표 검수 |
| 현지화 | 양쪽50키, UTF-8 BOM·헤더·키 대응·설명 정합성 확인 |
| 보존·정리 | 기존 아이콘 본체 보존, diff 공백 오류 없음; 임시 진단 파일은 실행 후 해시 대조 제거 |

직사각형 모델은 엔진 연결선의 실제 꺾임·이름판·배타 표시·SHOW/HIDE를 검증하지 않는다. 선언 순서 오류 두 건은 후속 정책 진단 및 진단 제거 후 새 프로세스의 로그에서 사라졌음을 확인했다. AGENTS.md의 현행 기계 좌표 링크도 336개 자료로 갱신하고, 이전 326개 JSON에는 역사 기준선 표시와 후속 자료 경로를 추가했다.

## 실행 검증

[기존326개 기준선](../HOK_KOREAN_FOCUS_VALIDATION_20260922.md)은 초기105 PASS/2 FAIL 및 진단 기대값 문제를 별도로 기록한다. 이 기록을 새10개 검증으로 재사용하지 않는다.

새 정책의 네 선택은 각기 새 게임에서 임시 이벤트로 완료·활성화했다. 이는 역사 경로, 정상35일 진행, 실제 버튼 비용 지불 검증이 아니다. 임시 파일은 사용자 저장에 적용하지 않고 작업한 게임을 종료한 뒤 해시를 대조해 제거한다. 매 실행의 설정 전후 사본·로그·진단 해시는 ignored 경로 `.local-artifacts/korean-focus-expansion/second-wave-20260922/`에 보존한다.

초기 정책 진단(V1)은 `hours = 1`로 분리한 이벤트들이 실제로 같은 게임 시각에 처리되어 의도한 경제 갱신 간격을 확보하지 못했다. 군수공장 산출과 가용 민공 전체값의 일부 기대가 맞지 않았다. 최초 실패 로그를 보존한 채 진단만 고쳤으며, 이 실패를 근거로 생산 코드를 변경하지 않았다. 설치본은 `hours` 문법을 지원하므로 문법 미지원으로 판정한 것이 아니다.

### 네 선택의 재검증

V2에서는 정부·오프사이트 공장·기존 진입 중점을 설정한 후 게임 날짜를 이틀 진행하고, 같은 정부 아래의 기준값과 새 정책의 직접 효과를 비교했다. 사업을 활성화한 뒤 민주정부로 바꾸고 다음 날 실제 자동 취소를 검사했다. 각 실행은 1936.1.1.12~1.4.11의 독립된 새 게임이며 저장하지 않았다.

| 정책 선택 | V1 PASS / FAIL | V2 PASS / FAIL | V2 실행 체크섬 |
|---|---:|---:|---|
| 여운형 공동구매 | 45 / 4 | 37 / 0 | `96df` |
| 여운형 지역금융 | 44 / 6 | 38 / 0 | `23de` |
| 박헌영 군수설비 확충 | 43 / 6 | 37 / 0 | `82a7` |
| 박헌영 현행 생산 | 45 / 4 | 37 / 0 | `3642` |
| 합계 | 177 / 20 | 149 / 0 | 임시 진단 파일이 서로 달라 체크섬도 다름 |

확인한 것은 해당 경로의 4개 중점 보상 실행, 선택한 정신의 I→II 교체와 하위 정신 제거, 미선택 정신 미지급, 사업 활성 상태·공통 사용 중 플래그, 직접 정책 효과와 `civilian_factory_use` +2, 정권 변경 후 정신·사업 제거·플래그 해제·대기 플래그 설정·공장 사용 보정 회수다. 지역금융은 하위 정신을 먼저 제거한 경우에도 선택한 상위 정신을 직접 지급하는 분기를 추가 검사했다.

가용 민공 전체값과 정권 변경 전후의 국가 전체 보정은 별도 관측값으로 보존했다. 네 V2 실행 모두 사업 활성 직후 가용 민공 -2가 관측됐지만, 이를 다른 경제 변수가 있는 모든 상황의 불변식으로 단정하지 않는다. 정권 변경 후 안정도·소비재와 산출 총합도 변하므로 공산정부의 기준값과 무조건 같아야 한다는 V1 기대를 제거했다. 검사 수가 줄어든 것은 이처럼 직접 효과 검사와 경제 총합 관측을 구분했기 때문이며, V1 실패를 숨기거나 전부 해결된 게임 결함으로 재분류한 것이 아니다.

`complete_national_focus`와 `activate_decision`을 사용한 진단이므로 정상 35일 진행, 배타 선택의 UI 차단, PP75의 실제 지불, 시작 조건 차단, 정상 90일 완료·90일 대기 만료·재착수는 증명하지 않는다. 149 PASS는 위에 열거한 실행 효과 검사에 한정된다.

### 임시 진단 제거 후 실행

임시 on_action·이벤트 두 파일을 해시 대조 후 제거하고 생산 파일만으로 새 프로세스를 실행했다. `system.log`에서 1.19.3.0.c01a·체크섬 `ac71`·일반판 모드 1개·DLC36개, `game.log`에서 1936.1.1.12 싱글플레이 진입을 확인했다. 약 80초 뒤 작업에서 시작한 프로세스만 종료했다. 화면을 직접 관측한 검증은 아니다.

- **CONFIRMED:** 기준선의 상대 기준 선행 선언 오류 2건은 V1·V2 및 진단 제거 실행에서 모두 사라졌다.
- V1에서 14건, V2에서 12건 발생한 `production_speed_industrial_complex_factor`·`production_speed_arms_factory_factor`의 dynamic token/OOS 경고는 진단 제거 실행에서 0건이었다. 임시 `modifier@…` 조회가 유발한 경고로 분리했다. 실제 멀티플레이 동기화 검증 결과를 뜻하지 않는다.
- 새 공산 정책·이미지의 명시적인 파싱·참조 오류는 비교 로그에서 발견되지 않았다. 기존 캐릭터·교리·기술·descriptor·음원 오류는 남았으므로 전체 로그가 정상이라고 판정하지 않는다.
- 시간 표기만 정규화해 비교하면 진단 제거 실행의 오류·경고는 128개 레코드로, 기준선 130개에서 상대 기준 오류 2건만 줄었다. 기존 `cl_tech`의 보고된 소스 행 번호 2264→2266 이동 외에는 메시지와 발생 횟수가 같았다.
- 네 V2 실행과 진단 제거 실행 모두 `dlc_load.json`의 전후 해시가 같았다. 사용자 플레이세트·설정·세이브를 변경하지 않았으며 진단 파일과 작업에서 실행한 게임 프로세스도 남기지 않았다.

실행 후 생산 파일 28개의 SHA256을 실행 전 스냅샷과 대조해 차이 0건을 확인했다. 임시 진단 파일 잔여 0건, 새 문서의 내부 링크 누락 0건, JSON 4개 파싱 및 `git diff --check`도 통과했다.

원문 증거는 ignored 경로의 `policy-results/`, `policy-results-v2/`, `clean-production/`에, 집계는 `policy-v1-summary.json`, `policy-v2-summary.json`, `runtime-error-comparison.json`에 보존했다. 체크섬은 각 진단 환경 식별값이며 기존 세이브나 멀티플레이 호환성 증거로 사용하지 않는다.

## 남은 범위

- 전체2차 명세 확정과 나머지 노선 구현. 최초134개 후보 중 반복적인 일회성 보상 단계를 재설계해야 한다.
- 정상 중점 진행·정치력 지불, 사업90일 정상 종료·반복, 여러 정책의 동시 실행 차단, 내전·항복·민공 감소, 저장 후 재로드.
- 기존 지역90일·동맹180일 사업 반복과 협정 수락·거절·만료; 정상 의존성 구성, 다른 DLC·북마크·기존 저장·멀티플레이.
- 장기 AI 진행과 보상 도달 시점, 실제 중점/결정 화면 및 잠금·진행·완료·광택 상태. 네이티브 UI 도구가 연결되지 않아 이번 작업에서 화면을 자동 조작·검수하지 못했다.

소스 적용·정적 검사·짧은 스크립트 실행은 전체 플레이 검증 완료를 뜻하지 않는다. 커밋·푸시·업로드는 하지 않았다.
