# 환제국 H1 — 오른쪽 상단 공간으로 이동

작성: 2026-09-23. 사용자 요청의 범위는 H1 네 중점을 오른쪽 빈 공간에 묶어 배치하고 시작 높이를 `계연수 축출`과 맞추는 것이다. 위치 변경만 적용하며 보상·진행·표시 조건은 보존한다. 앞선 설명 단계의 x214는 잠정 예시였으며 최종 적용값은 아래와 같다.

## 기준선과 환경

브랜치 `feat/korean-focus-expansion-ai`, HEAD `3f3b70686175516469f7ae51fd34a12240c871d9`에서 기존 미커밋 460개 작업을 보존했다. 현재 로컬 launcher `.mod`는 저장소 `C:/hoi/hearts_of_korea`를 직접 읽는다. 원작 Workshop `2898629778`과 설치본은 읽기 전용 참고 자료다. 저장소·launcher descriptor의 `Korean Language` 의존성, `replace_path` 없음, 기존 `remote_file_id=3793992662`는 이번 배치에서 변경하지 않았다. 업로드·메타데이터 변경은 수행하지 않는다.

| 항목 | 기록 |
|---|---|
| 대상 설치본 | HOI4 `1.19.3.0.c01a`, Steam build `25205862` |
| 수정 전 사용자 로그 | 체크섬 `0af0`; 새 실행과 구분해 보존 |
| 운영체제·언어 | Windows 11, `l_korean` |
| 사용자 데이터 | `C:/Users/jaewo/OneDrive/문서/Paradox Interactive/Hearts of Iron IV` |
| 실행 구성 | 로컬 Hearts of Korea 하나 활성, DLC 36개 활성; descriptor 의존성 선언과 실제 활성 목록을 구분 |
| 기록된 화면 설정 | 1600×900, GUI scale 1.0; 이번 위치의 실제 화면 관측값은 아님 |
| 직전 중점 소스 SHA-256 | `dd2334a0f5be074c0e52adb280b6cbeb80a20412ccbcee62f754adf4e85613b4` |
| 적용 중점 소스 SHA-256 | `1cedaf78903db31d15e3837e20069b71bca8fc674212b1d033f0d721b2b6735c` |

## 변경과 근거

`CONFIRMED`: 사용자 화면과 요청은 입헌군주정 실시 오른쪽 상단의 공간을 이용하는 배치를 가리킨다. `계연수 축출`의 기본 좌표는 (196,5)다. H1 시작은 같은 y=5에 두되 기존 제국섭정↔계연수 축출 상호배타선 끝의 바깥으로 옮겼다. 왕의 귀환의 기본 (186,4)와 선언 순서를 유지하여 시작 상대값은 `x=-1,y=6`에서 `x=25,y=1`로 바꿨다. 시작의 기여 노트와 이 두 좌표 외에 생산 소스를 바꾸지 않았다.

| 중점 | 직전 기본 위치 | 현재 기본 위치 | 왕정 완료 HIDE 계산 위치 |
|---|---|---|---|
| 환국 지방실사 `HOK_KOR_hw_provincial_inventory` | (185,10) | (211,5) | (110,5) |
| 지방 물자관리 `HOK_KOR_hw_provision_accounts` | (184,11) | (210,6) | (109,6) |
| 제국 지방 순찰 `HOK_KOR_hw_inspection_circuits` | (186,11) | (212,6) | (111,6) |
| 물자 결산통일 `HOK_KOR_hw_supply_returns` | (185,12) | (211,7) | (110,7) |

세 자식의 시작 기준 상대좌표 (−1,1)/(1,1)/(0,2)와 시작 → 두 병렬 정책 → AND 합류의 3행 대칭 구조를 유지한다. 왕정 HIDE의 −101x는 기존 기준점 연쇄로 한 번만 상속하며 새 offset은 없다. SHOW·선택 전·진행 중은 기본 위치다. 전체 범위는 x0~211/y0~24에서 **x0~212/y0~24**로 오른쪽 끝만 1칸 늘고, 왕정 완료 HIDE 계산 범위는 x75~110에서 x75~111이 된다.

기존 폭 안의 시작 x208·209·210 후보도 검토했으나, y7 합류 중점에 C2 사법행정 진입선이 닿는 보수적 모델 후보가 있었다. 시작 x211은 3행 모양을 보존하면서 그 후보를 피한다. 이는 모델에 따른 배치 선택이며 엔진이 그린 선의 정확한 꺾임이나 픽셀 간격을 확정한 결과는 아니다. 전체 트리 간격을 늘리거나 다른 중점·패널·바로가기를 옮기지 않았다.

## 정적 검증과 실제 검증의 구분

| 검증 | 결과·한계 |
|---|---|
| 소스 보존 | **10 PASS / 0 FAIL**. ID·선언 순서·다른 459개 블록 원문·시작의 배치 외 순서 있는 토큰·UTF-8 무BOM/CRLF를 보존. 변경은 시작 좌표 두 값과 기여 노트뿐 |
| 좌표 보존 | 460개, 절대 기준 7개·상대 453개·기존 직접 offset 7개 유지. H1 외 **456개 기본 좌표** 불변, H1은 기존 기준을 통해 한 번 이동 |
| SHOW/HIDE 모델 | 20개 상태에서 H1 관련 중점 사각형 겹침·세 직교 선행선 모델 접촉·무관 가로 공선·상호배타선 높이 영역 접촉 모두 0건. DDS 크기와 8px 여유를 쓴 보수적 정적 모델이며 모든 선 교차·실제 클릭 영역의 검증이 아님 |
| 실제 SHOW/HIDE 화면 | **미검수**. native computer-use 연결이 초기화·재초기화 후에도 pipe 연결 장애로 두 번 실패하여 현재 위치·선·이름판·스크롤·바로가기를 실제 화면으로 확인하지 못함 |
| 진단 없는 일시정지 새 게임 로드 | **통과**. 전체 재시작한 `clean-hide-final`은 34.42초에 한국 1936.1.1 12시 일시정지 로드 완료를 로그로 확인했다. 체크섬 `8a72`, 초기 HIDE는 설정·로그에 따른 상태이며 실제 화면 관측이 아님. 임시 준비 진단 없이 실행 |
| 로그 비교 | 수정 전 보존 로그 130행·고유 41개 대비 실행 후 128행·고유 40개. 시간 필드 정규화 후 신규 메시지 0개·발생 횟수 증가 0개. 사라진 2회는 기존 `random.log` 이름 변경 실패이며 배치 수정의 효과로 해석하지 않음 |
| 사용자 환경·정리 | 저장 13개·플레이세트·launcher·생산 파일 150개 해시 유지. 소유한 검증 프로세스 종료, 정리 오류 0. 설정은 엔진의 `graceful_exit=no` 추가만 달라 `SettingsUnchanged=false`; 도구가 설정을 쓰거나 복원하지 않았으며 바이트 동일로 기록하지 않음 |
| 보상·정상 진행 | 이번 좌표 변경에서 재실행하지 않음. 선행·AND·비용·보상·취소·표시·AI·그림·현지화는 소스 대조로 보존 확인 |

직전 (185,10)의 `dd2334a0…85613b4`는 별도 새 게임의 초기 SHOW·왕정 완료 합성 HIDE에서 관측됐지만 이는 [이전 배치 기록](2026-09-23-hwan-focus-layout.md)의 증거다. 이번 (211,5)의 실제 화면 통과로 옮기지 않는다. 잔여 82개 통합 당시 `905a3868…1ccf852`의 정적·강제 보상·정상 진행·로드 기록도 현재 소스 재실행과 구분한다.

PowerShell 스크립트는 실행 정책으로 차단됐고 최초 sandbox의 `tasklist` 접근도 거부됐다. 보안 설정을 바꾸지 않고 승인된 권한 확장의 Python 실행기로 위 로드 검증을 완료했다. 이 초기 실행 실패와 native UI 연결 장애는 구분한다. 좌표 JSON의 전체 460개 노드와 후보 JSON의 명시적 위치 122개도 생산 좌표와 일치하며 직전 `dd2334a0…85613b4`의 UI 기록은 역사적 검증으로 보존했다.

`STRONGLY_SUPPORTED`: 현재 좌표는 요청한 오른쪽 상단 위치와 같은 시작 높이를 충족하며 모델상 H1 간섭 후보가 없다. `UNPROVEN`: 실제 SHOW/HIDE의 선·명판 분리, 정치 선택 전후 이동 장면, 숨은 기준점, 끝부분 스크롤·패널 접근. 전체 정상 진행·장기 AI·이전 저장·다른 DLC·멀티플레이는 이번 검증 범위가 아니다.

## 재현 자료와 Git 상태

다음은 커밋하지 않는 로컬 자료다.

- `.local-artifacts/incidents/2026-09-23-hwan-focus-right/before/`: 변경 전 관련 소스·문서·사용자 설정과 로그 보존.
- 같은 폴더의 `baseline.json`, `production-hashes.json`, `preservation.json`: 대상 환경·소스 해시·10개 보존 검사.
- 같은 폴더의 `log-comparison.json`, `runtime-results/clean-hide-final/run.json`: 진단 없는 새 게임 로드·로그 대조·사용자 환경 보존과 정리 결과.
- `.local-artifacts/korean-focus-expansion/remaining-20260923/h1-rightspace-review-agent.json`, `h1-rightedge211-review-agent.json`: 후보 검토와 현재 시작 x211의 20상태 모델 결과.

현재 배치는 [좌표 명세](../HOK_KOREAN_FOCUS_LAYOUT_COORDINATES.md), [좌표 JSON](../data/HOK_KOREAN_SECOND_WAVE_COORDINATES.json), [왕정 명세](../HOK_KOREAN_ROYAL_FOLLOWUP_SPEC.md)에 반영한다. 이번 범위 밖 기존 작업은 유지했으며 commit·push·게시·업로드는 수행하지 않았다. 실제 UI 검수는 도구 연결이 복구된 뒤 현재 소스의 별도 재시작에서 완료해야 한다.
