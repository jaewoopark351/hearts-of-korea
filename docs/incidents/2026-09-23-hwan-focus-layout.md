# 환제국 H1 — 상호배타 연결선의 이름판 관통 수정

> **이전 배치 기록:** 이 문서의 (185,10) 배치와 SHOW/HIDE 관측은 소스 `dd2334a0…85613b4`에만 해당한다. 후속 사용자 요청으로 H1을 오른쪽 상단 (211,5) → (210,6)/(212,6) → (211,7)로 옮겼다. 현재 소스·좌표·검증 상태는 [오른쪽 배치 기록](2026-09-23-hwan-focus-right.md)을 따른다. 아래 실제 화면 관측을 새 위치의 검증으로 재사용하지 않는다.

작성: 2026-09-23. 사용자 화면에서 확인한 배치 결함 수정이며 보상·진행 규칙의 변경이 아니다. 브랜치 `feat/korean-focus-expansion-ai`, HEAD `3f3b70686175516469f7ae51fd34a12240c871d9`의 미커밋 460개 작업을 보존했다.

| 소스 | `common/national_focus/korea.txt` SHA256 |
|---|---|
| 수정 전 460개 | `905a3868776f4b5aedc1e5f41bda0568bf279f38a48343888114e985f1ccf852` |
| H1 배치 수정 후 | `dd2334a0f5be074c0e52adb280b6cbeb80a20412ccbcee62f754adf4e85613b4` |

## 확인한 원인

사용자 스크린샷에서 기존 `제국섭정 계연수`와 `계연수 축출` 사이의 상호배타 가로선이 신규 `환국 지방실사`(`HOK_KOR_hw_provincial_inventory`) 이름판을 통과했다. 기존 두 중점은 기본 `(178,5)`와 `(196,5)`, 신규 시작은 `(183,5)`여서 가로선의 같은 행 안에 놓였다. 중점 사이의 진행 선행이 잘못 연결된 문제가 아니다.

이전 정적 모델은 아이콘·이름판 사각형과 세 가지 직교 선행선 모델을 검사했지만 **상호배타 연결선을 별도 모델에 포함하지 않았다.** 그래서 이전 20개 상태의 선 접촉 0건을 이 화면의 안전성으로 확대할 수 없었다. 잔여 확장 당시 실제 화면 검수에도 H1 전체는 포함되지 않았다. 이전 정적 896/0·좌표 335/0와 즉시 보상 1,360/0는 [당시 소스의 기록](2026-09-23-korean-remaining-followup.md)으로 보존하며 이번 배치의 검증 결과로 재사용하지 않는다.

## 적용한 최소 변경

시작 중점의 위치 기준은 기존 `KOR_return_of_the_king` `(186,4)`를 유지하고 상대 `x=-3,y=1`만 **`x=-1,y=6`**으로 변경했다. 자식 세 개의 상대좌표와 기준은 그대로 두어 H1 전체가 함께 이동한다.

| H1 중점 | 이전 기본 위치 | 수정 기본 위치 | 왕정 완료 HIDE 위치 |
|---|---|---|---|
| 환국 지방실사 | (183,5) | (185,10) | (84,10) |
| 지방 물자관리 | (182,6) | (184,11) | (83,11) |
| 제국 지방 순찰 | (184,6) | (186,11) | (85,11) |
| 물자 결산통일 | (183,7) | (185,12) | (84,12) |

왕정 HIDE의 −101x를 원래 기준점에서 한 번 상속하며 새 offset은 없다. 시작 → 두 병렬 정책 → AND 합류의 3행 구조, 실제 환제국 형성·운영 영토 조건, 보상·35일 비용·AI·그림·현지화·다른 선행은 보존한다. 수정된 생산 블록은 시작 중점 하나뿐이고 나머지 459개 블록은 원문 동일하다. 이 중 상대 자손 세 개의 화면 위치만 따라 움직이므로 나머지 **456개 좌표**는 유지된다. 전체 x0~211·y0~24, 절대 기준 7개·상대 453개, 기존 offset·패널·바로가기도 유지된다.

## 검토한 대안과 정적 검증

아래 수치는 초기 SHOW의 후보별 정적 결과다. 다른 표시 상태와 실제 화면을 대신하지 않는다.

| 안 | 중점 사각형 겹침 | 선행선 접촉 | 무관한 가로선 공선 | 배타선 이름판 높이 접촉 | 판단 |
|---|---:|---:|---:|---:|---|
| 이전 위치 | 0 | 0 | 0 | 1 | 사용자 지적을 새 회귀 모델로 재현 |
| 전체를 한 행 아래로 | 1 | 2 | 1 | 0 | 마지막 중점이 기존 독일 협력 중점과 겹치고 중간 이름판에 기존 선이 닿음 |
| 시작만 (183,4)로 이동 | 0 | 2 | 2 | 0 | 왕의 귀환→제국섭정 선이 시작 이름판에 닿고 동일행 부모 연결도 안전하지 않음 |
| 시작 (186,6)의 3행 구조 | 0 | 3 | 2 | 0 | 비변사에서 갈라지는 기존 선과 새 중간·합류 선이 간섭 |
| 채택: 시작 (185,10)의 3행 구조 | 0 | 0 | 0 | 0 | 기존 조건·기준을 유지하며 배타선 영역을 벗어남 |

별도 생산 변경 보존 검사는 **10 PASS / 0 FAIL**이다. 채택안은 실제 H1 DDS 크기와 8px 여유, 기존 20개 SHOW/HIDE 상태에서 중점 겹침·세 선행선 모델 접촉·무관 공선·배타선 높이 영역 접촉 모두 0건이다. 배타선 모델은 사용자 화면의 이름판 관통에 근거해 격자 중심 아래 20~70px 높이 영역을 보수적으로 검사하며 엔진 픽셀 위치를 정확히 재현했다는 뜻은 아니다. 길어진 진입선은 아래 두 실제 화면에서도 별도로 확인했다. 직교 선끼리의 모든 교차를 금지하거나 0건이라고 주장하지 않는다.

## 실제 게임 검증

| 실행 | 상태·범위 |
|---|---|
| 초기 SHOW | `show-01` 준비 검사 1 PASS / 0 FAIL. 전체 재시작한 1936.1.1 12시 일시정지 상태, HOI4 1.19.3·체크섬 `565a`, 1600×900·GUI 1.0에서 왕정 바로가기 배율 0.65 및 확대 버튼 3회로 검수. 기존 배타선 영역에 H1 이름판이 남지 않고 옮긴 네 아이콘·명판·분기·합류가 주변과 분리됨을 확인. 긴 진입선은 독일 협력 명판 옆으로 지나 가리지 않지만 기존 선과의 교차 자체는 남음. 시작 이름판 클릭으로 올바른 중점과 35일·산업 연구 150% 툴팁 확인 |
| 왕정 완료 HIDE | `hide-01` 준비 검사 1 PASS / 0 FAIL. 별도 새 게임 1936.1.1 12시 일시정지·체크섬 `820a`, 1600×900·GUI 1.0에서 왕정 시작 중점만 보상 없이 합성 완료했다. 서로 다른 확대/스크롤의 두 화면에서 기존 배타선 행에 H1 이름판이 없고 옮긴 네 아이콘·명판·진입·분기·합류가 주변을 가리지 않음을 확인. 환제국 형성·H1 보상은 실행하지 않았으며 중간 사용자 입력 뒤 최신 화면을 다시 확보해 판정 |
| 사용자 환경 보존·정리 | 두 실행 모두 저장 13개·플레이세트·런처·production 동일, 정리 오류 0이며 소유한 검증 프로세스·진단을 정리했다. SHOW 설정은 엔진이 `graceful_exit=no` 한 줄만 추가하여 `SettingsUnchanged=false`, `OnlyGracefulExitChanged=true`다. 도구가 설정을 쓰거나 복원하지 않았으며 바이트 동일로 기록하지 않음. HIDE는 설정도 전후 동일 |
| 오류 로그 비교 | 직전 사용자 세션의 보존 로그와 SHOW·HIDE가 각각 비어 있지 않은 128행·고유 메시지 40개다. 두 시각 필드를 정규화해 신규 메시지 0개·발생 횟수 증가 0개. 새 진단 없는 대조군이 아니며 기존 오류가 사라졌다는 뜻도 아님. SHOW `565a`와 HIDE `820a`는 서로 다른 임시 표시 준비 진단을 포함한 실행 체크섬 |
| 진단 없는 추가 로드 | `NOT_RERUN_FOR_LAYOUT_ONLY_REPAIR`. 이번 두 실행은 표시 상태를 준비하는 임시 진단이 있었으며 진단 없는 새 로드로 표현하지 않음. 이전 진단 없는 로드는 수정 전 905a 소스의 결과 |

위 두 실행은 일시정지한 별도 표시 상태이며 정상 정치 선택 전→완료 후 이동 장면이나 새 보상 진행 검사가 아니다. 이전 905a 소스의 정상 진행·즉시 보상 검사는 이번 좌표 변경에서 게임 규칙이 보존됐다는 소스 대조와 함께 참고할 수 있으나 수정 소스에서 재실행했다고 기록하지 않는다. AI·저장·다른 DLC·멀티플레이의 미검증 범위도 유지한다.

## 재현 자료

다음은 커밋하지 않는 로컬 자료다.

- `.local-artifacts/incidents/2026-09-23-hwan-focus-layout/preservation.json`: 수정 전후 생산 소스의 10개 보존 검사와 새 좌표.
- 같은 폴더의 `freeze-metadata.json`, `production-hashes.json`: 수정 소스의 150개 생산 파일 해시와 브랜치·커밋.
- 같은 폴더의 `log-comparison.json`: 보존된 사용자 세션과 두 UI 실행의 정규화 오류 메시지·횟수 비교.
- `.local-artifacts/korean-focus-expansion/remaining-20260923/h1-layout-review.py`와 `h1-layout-review.json`: 이전 배타선 관통 재현, 대안 비교, 채택안의 20개 조건 모델. 소스는 수정하지 않고 ignored 결과만 기록한다.
- 배치 수정 폴더의 `runtime-results/show-01`, `runtime-results/hide-01`: 실행별 `run.json`, `ui-observations.json`, 로그·환경 보존 결과. 화면은 대화 중 직접 검토했으며 관측 기록을 보존한다.

현재 배치와 구현 데이터는 [좌표 명세](../HOK_KOREAN_FOCUS_LAYOUT_COORDINATES.md), [좌표 JSON](../data/HOK_KOREAN_SECOND_WAVE_COORDINATES.json), [왕정 명세](../HOK_KOREAN_ROYAL_FOLLOWUP_SPEC.md)를 따른다.
